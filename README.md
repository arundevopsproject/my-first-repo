#This is a readme file, I have just created from github

#DevOPs NGINX Demo

#Steps


#Installation
sudo apt update -y
sudo apt install nginx

#Service start & enable
sudo systemctl start nginx
sudo stetenctl enable nginx
sudo systemctl status nginx

